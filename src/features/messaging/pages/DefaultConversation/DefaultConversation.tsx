import classes from './DefaultConversation.module.scss';

const DefaultConversation = () => {
  return (
    <div className={classes.root}>
      Start a conversation now
    </div>
  );
};

export default DefaultConversation;